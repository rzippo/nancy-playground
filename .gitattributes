# Every text file is stored and checked out with LF.
# Tools that write the line endings of the platform they run on, like the ANTLR generator on Windows,
# would otherwise rewrite every line of the files they touch.
* text=auto eol=lf

*.png binary
